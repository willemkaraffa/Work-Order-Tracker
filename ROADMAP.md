TRACKER PROGRAM ROADMAP

FIX: Preflight returns errors
FIX: Detail Panel note card three-dot drop down non-functional: edit, delete, pin
FIX: Manage Statuses sometimes does not allow editing of names/entering new status
FIX: Ability to reorganize all Workflow types/statuses missing
FIX: Sync to Workbook not working, fix or find superior alternative
FIX: revert legacy feature where tracker asks confirmation for extension imports. Ensure imports go where they are supposed to.
CHANGE: Allow per Phase sorting of WOs by status/age/etc. ascending/descending Sort dropdown per Phase Header. Alternatively, add this sorting to the main Sort feature instead. Whichever is cleaner.
CHANGE: Allow sorting (ascending/descending) of WOs by last note date.
CHANGE: Allow the status of WOs to be changed without having to go into the edit form. detail pane dropdown and right click context menu 
CHANGE: Make the Work Type symbol on the list panel more noticeable
CHANGE: Allow for multiple selection of list items to accommodate more actions, like mass-changing status with right click
FEATURE: Introduce right-click context menu to perform more actions from the list panel
FEATURE: Introduce the custom filters and pages in the left panel that were in earlier builds, such as a separate tracker page for a technician's daily routes. Accessible via Tools dropdown.
CHANGE: Relocate status pills on rows to far left before Company pills, currently makes rows feel cluttered can harder to parse
CHANGE: Change format of City on row to always align right; all city tags should be in the same position in their particular row to the left of the day counter.
CHANGE: Scraper should be able to pull all address data; Imported rows should only list street address field, and place city in city field some rows already do. This should be standard, and any rows with city in the address field should have the city/zipcode deleted from the address and moved to City field, replacing text in city field, if present.
UPDATE: Continue work on reliable, accurate, and secure scraper for work order information and import code to spreadsheet as well.
